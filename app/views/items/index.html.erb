<%= link_to '購入画面へ行く', new_product_path %><br>
<div class="contents">	
  <div class="content">
    <% @items.each do |item| %>
      <p><%= item.name %></p>
      <p><%= item.price %>円</p>
    <% end %>
  </div>
</div>